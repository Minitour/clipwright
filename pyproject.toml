[project]
name = "video-master"
version = "0.1.0"
description = "Shared workspace Python environment (ManimGL and other tooling)"
requires-python = ">=3.13"
dependencies = [
    "audioop-lts>=0.2.2",
    "kokoro>=0.9.2",
    "manimgl>=1.7.2",
    "setuptools>=69,<81",
    "soundfile>=0.13.1",
]
